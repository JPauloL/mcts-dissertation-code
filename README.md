### Monte Carlo Tree Search

- The benchmark project contains code that is able to run and compare algorithms on the DIMACS graph coloring instances;
- The mcts project contains the code of the Monte Carlo Tree Search;

### Static Type Check

To run mypy in the venv:
```
uv run mypy .
```